#include <iostream>

using namespace std;

// p07, identifier
int main() {

    cout << "identifier" << endl;

    // cannot use keyword as identifier
    //int int = 0;

    // can only use alphabet character, digit or underscore in identifier
    //int %d = 0;

    // the first character must be an alphabet character or underscore in identifier
    //int 90z = 0;
    
    // upper case and lower case are different in indentifier
    //int Aa = 0;
    
    //system("pause");    // use it in windows, effect [press any key to continue], no such command in linux/mac

    return 0;

}
