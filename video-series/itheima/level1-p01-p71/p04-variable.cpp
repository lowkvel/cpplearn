#include <iostream>

using namespace std;

// p04, variable
int main() {

    cout << "variable" << endl;

    // variable declaration: variable_type variable_name = initial_value;
    int a = 10;
    cout << "int a = " << a << endl;
    
    //system("pause");    // use it in windows, effect [press any key to continue], no such command in linux/mac

    return 0;

}
